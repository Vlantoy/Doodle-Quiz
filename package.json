{
  "name": "cute-quiz-pvp",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
